// Simple QNode test file
console.log("QNode is working!");

// Test basic JavaScript features
let x = 10;
let y = 20;
console.log(`${x} + ${y} = ${x + y}`);

// Test functions
function greet(name) {
    return `Hello, ${name}!`;
}

console.log(greet("QNode"));

// Test arrays
const arr = [1, 2, 3, 4, 5];
console.log("Array sum:", arr.reduce((a, b) => a + b, 0));

// Test objects
const obj = { name: "QNode", version: 1.0 };
console.log("Object:", obj);
